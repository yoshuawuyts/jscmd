/**
 * Module dependencies.
 */

var exec = require('');

/**
 * Expose `cmd()`.
 */

module.exports = cmd;

/**
 * Run a given tool with arguments.
 *
 * @param {String} name
 * @param {String[]?} args
 */

function cmd(name, args) {

}
